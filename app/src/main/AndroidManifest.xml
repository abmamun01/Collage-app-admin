<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamunsproject.awesome_collage_application">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Awesome_Collage_Application">
        <activity android:name=".Notice.Delete_Notice_Activite"></activity>
        <activity android:name=".Update_Teacher" />
        <activity android:name=".Faculty.AddTeacher" />
        <activity android:name=".Faculty.Update_Faculty" />
        <activity android:name=".Upload_Pdf" />
        <activity android:name=".Upload_Image" />
        <activity android:name=".Notice.Upload_Notice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>